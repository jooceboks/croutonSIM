# Postgres connection. If DATABASE_URL is unset, the app falls back to:
#   postgres://localhost/crouton_sim
# which is the local Homebrew Postgres used in development.
#
# To point somewhere else, export DATABASE_URL in your shell before running scripts:
# DATABASE_URL=postgres://user:pass@host:5432/crouton_sim
